{
  "name": "social_bee",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "server": "cd backend && npm run server",
    "dev": "concurrently -n \"frontend,backend\" -c \"bgMagenta,bgGreen\" \"npm run frontend\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}